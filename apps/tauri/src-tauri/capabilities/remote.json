{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "remote-access",
  "description": "Allow the default remote webapp (app.getcore.me) to use Tauri APIs. Self-hosted URLs configured in ~/.corebrain/config.json are granted equivalent access at runtime via add_capability in lib.rs.",
  "remote": {
    "urls": ["https://app.getcore.me"]
  },
  "windows": ["main", "voice", "inbox"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "notification:default"
  ]
}
